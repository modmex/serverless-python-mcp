[tool.poetry]

name = "mcp-rest-api-streaming-example"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]

python = ">=3.11,<4.0"
modmex-lambda = "0.7.2"

[build-system]

requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.requires-plugins]
poetry-plugin-export = ">=1.8"
